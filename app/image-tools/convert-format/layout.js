export const metadata = {
  title: "Image Format Converter — Convert JPG PNG WebP Free",
  description: "Free image format converter — convert images between JPG, PNG and WebP formats. Quality control slider included. No signup, no upload to server.",
};

export default function Layout({ children }) {
  return children;
}
