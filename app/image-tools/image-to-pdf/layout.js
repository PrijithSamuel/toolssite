export const metadata = {
  title: "Image to PDF Converter — Convert Images to PDF Free",
  description: "Free image to PDF converter — convert JPG, PNG and WebP images to PDF. Multiple images supported. Choose page size and orientation. No signup required.",
};

export default function Layout({ children }) {
  return children;
}
