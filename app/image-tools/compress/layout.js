export const metadata = {
  title: "Image Compressor — Reduce Image File Size Free",
  description: "Free online image compressor — compress JPG, PNG and WebP images without losing quality. Reduce image file size by up to 70%. No signup, no upload to server.",
};

export default function Layout({ children }) {
  return children;
}
