export const metadata = {
  title: "Image Resizer — Resize Images Online Free",
  description: "Free image resizer — resize JPG, PNG and WebP images to any dimension. Preset sizes for HD, Full HD and social media. Aspect ratio lock included.",
};

export default function Layout({ children }) {
  return children;
}
