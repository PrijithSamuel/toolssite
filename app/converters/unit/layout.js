export const metadata = {
  title: "Unit Converter — Length Weight Temperature Speed Free",
  description: "Free unit converter — convert length, weight, temperature, speed, area and volume. Metric and imperial units. Instant results, no signup required.",
};

export default function Layout({ children }) {
  return children;
}
