export const metadata = {
  title: "Color Converter — HEX RGB HSL Color Code Converter",
  description: "Free color converter — convert HEX color codes to RGB and HSL instantly. Color picker included. Copy any format with one click.",
};

export default function Layout({ children }) {
  return children;
}
