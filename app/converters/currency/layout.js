export const metadata = {
  title: "Currency Converter — Convert Between 20 Currencies Free",
  description: "Free currency converter — convert between 20 world currencies including USD, EUR, GBP, INR, AUD, JPY and more. Reference rates, no signup needed.",
};

export default function Layout({ children }) {
  return children;
}
