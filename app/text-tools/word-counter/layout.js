export const metadata = {
  title: "Word Counter — Count Words Characters Free Online",
  description: "Free word counter — count words, characters, sentences, paragraphs and estimate reading time. Instant results as you type. No signup required.",
};

export default function Layout({ children }) {
  return children;
}
