export const metadata = {
  title: "Remove Duplicate Lines — Free Online Text Tool",
  description: "Free duplicate line remover — paste any text and remove duplicate lines instantly. Options for case-sensitive matching and whitespace trimming.",
};

export default function Layout({ children }) {
  return children;
}
