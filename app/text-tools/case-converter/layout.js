export const metadata = {
  title: "Case Converter — UPPER lower Title Sentence Case Free",
  description: "Free case converter — convert text to uppercase, lowercase, title case, sentence case and alternating case instantly. No signup required.",
};

export default function Layout({ children }) {
  return children;
}
