export const metadata = {
  title: "Body Fat Calculator — Estimate Body Fat Percentage Free",
  description: "Estimate body fat percentage using the US Navy method based on waist, neck and height measurements. Includes healthy body fat ranges by age and gender.",
};
export default function Layout({ children }) {
  return children;
}
