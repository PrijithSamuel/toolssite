export const metadata = {
  title: "EMI Calculator — Loan Monthly Payment Calculator",
  description: "Free EMI calculator — calculate your monthly loan payment, total interest and payment breakdown instantly. Home loan, car loan, personal loan EMI calculator.",
};

export default function Layout({ children }) {
  return children;
}
