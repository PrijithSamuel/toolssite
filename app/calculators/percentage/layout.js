export const metadata = {
  title: "Percentage Calculator — Calculate % Three Ways Free",
  description: "Free percentage calculator — what is X% of Y, X is what % of Y, percentage change from X to Y. Instant results, no signup required.",
};

export default function Layout({ children }) {
  return children;
}
