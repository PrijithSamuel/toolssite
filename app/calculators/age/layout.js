export const metadata = {
  title: "Age Calculator — Calculate Exact Age in Years Months Days",
  description: "Free age calculator — calculate your exact age from date of birth in years, months and days. Shows days until next birthday and total days lived.",
};

export default function Layout({ children }) {
  return children;
}
