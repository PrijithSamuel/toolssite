export const metadata = {
  title: "Scientific Calculator Online — Free Advanced Calculator",
  description: "Full scientific calculator with trigonometry (sin, cos, tan), logarithms, exponents, factorial and memory functions. No download needed — works in any browser.",
};
export default function Layout({ children }) {
  return children;
}
