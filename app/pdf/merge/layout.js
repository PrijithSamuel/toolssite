export const metadata = {
  title: "Merge PDF Files Online — Free No Watermark",
  description: "Free PDF merger — combine multiple PDF files into one. No watermark, no signup, no file upload to server. Reorder pages before merging. Works in your browser.",
};

export default function Layout({ children }) {
  return children;
}
