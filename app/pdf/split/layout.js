export const metadata = {
  title: "Split PDF Online Free — Extract PDF Pages",
  description: "Free PDF splitter — split PDF into individual pages or extract specific page ranges. No signup required, no watermark. Runs in your browser.",
};

export default function Layout({ children }) {
  return children;
}
