export const metadata = {
  title: "Compress PDF Online Free — Reduce PDF File Size",
  description: "Free PDF compressor — reduce PDF file size instantly without losing quality. No signup, no watermark, no upload to server. Works in your browser.",
};

export default function Layout({ children }) {
  return children;
}
