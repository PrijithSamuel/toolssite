export const metadata = {
  title: "Regex Tester — Test Regular Expressions Online Free",
  description: "Free regex tester — test and debug regular expressions with live match highlighting. Supports all JavaScript regex flags. Common patterns included.",
};

export default function Layout({ children }) {
  return children;
}
