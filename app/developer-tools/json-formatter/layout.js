export const metadata = {
  title: "JSON Formatter — Beautify Validate JSON Online Free",
  description: "Free JSON formatter — format, minify and validate JSON instantly. Paste your JSON to beautify or compress it. Shows exact error location for invalid JSON.",
};

export default function Layout({ children }) {
  return children;
}
