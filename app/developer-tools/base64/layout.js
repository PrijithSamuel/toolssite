export const metadata = {
  title: "Base64 Encoder Decoder — Free Online Tool",
  description: "Free Base64 encoder and decoder — encode text to Base64 or decode Base64 strings back to text instantly. No signup required.",
};

export default function Layout({ children }) {
  return children;
}
