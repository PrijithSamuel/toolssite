export const metadata = {
  title: "Password Generator — Strong Secure Passwords Free",
  description: "Free password generator — generate strong, secure passwords with uppercase, lowercase, numbers and symbols. Adjustable length up to 64 characters.",
};

export default function Layout({ children }) {
  return children;
}
