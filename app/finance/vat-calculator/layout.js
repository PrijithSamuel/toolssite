export const metadata = {
  title: "VAT Calculator — Add or Remove VAT at Any Rate",
  description: "Calculate VAT for any amount and custom rate. Add VAT to a net price or extract VAT from a gross total. Free online VAT calculator with inclusive and exclusive modes.",
};
export default function Layout({ children }) {
  return children;
}
