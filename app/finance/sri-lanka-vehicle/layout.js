export const metadata = {
  title: "Sri Lanka Vehicle Import Duty Calculator 2025",
  description: "Calculate import duty, excise duty, VAT and permit fees for importing a vehicle into Sri Lanka. Based on 2025 Customs rates and engine capacity bands.",
};
export default function Layout({ children }) {
  return children;
}
