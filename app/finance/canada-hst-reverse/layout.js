export const metadata = {
  title: "Canada Reverse HST Calculator 2025 — Remove Tax from Total",
  description: "Back-calculate GST/HST to find the pre-tax price from a tax-inclusive total for any Canadian province. Free online reverse tax calculator.",
};
export default function Layout({ children }) {
  return children;
}
